---
# roles/nginx/tasks/package_install.yml
# Nginx 包管理器安装：通过 apt/yum 安装系统自带的 Nginx，并应用自定义配置

- name: 安装 Nginx - RedHat 系
  ansible.builtin.yum:
    name:
      - nginx
    state: present
    update_cache: true
  when: ansible_os_family == "RedHat"
  tags:
    - package
    - install

- name: 安装 Nginx - Debian 系
  ansible.builtin.apt:
    name:
      - nginx
    state: present
    update_cache: true
  when: ansible_os_family == "Debian"
  tags:
    - package
    - install

- name: 确保 Nginx 配置目录存在
  ansible.builtin.file:
    path: "{{ nginx_conf_dir | default('/etc/nginx') }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: 确保 Nginx 日志目录存在
  ansible.builtin.file:
    path: "{{ nginx_log_dir }}"
    state: directory
    owner: "{{ nginx_user }}"
    group: "{{ nginx_group }}"
    mode: "0755"

- name: 确保 Nginx PID 目录存在
  ansible.builtin.file:
    path: "{{ nginx_pid_dir | default('/run') }}"
    state: directory
    owner: "{{ nginx_user }}"
    group: "{{ nginx_group }}"
    mode: "0755"

- name: 确保 /run/nginx 目录 SELinux 上下文正确
  ansible.builtin.file:
    path: /run/nginx
    state: directory
    setype: httpd_var_run_t
    seuser: system_u
    owner: "{{ nginx_user }}"
    group: "{{ nginx_group }}"
    mode: "0755"
  when: ansible_selinux.status == 'enabled'

- name: 生成 Nginx 主配置文件
  ansible.builtin.template:
    src: nginx.conf.j2
    dest: "{{ nginx_conf_dir | default('/etc/nginx') }}/nginx.conf"
    owner: root
    group: root
    mode: "0644"
  notify: restart nginx

- name: 清空默认站点配置（避免冲突）
  ansible.builtin.file:
    path: "{{ nginx_conf_dir | default('/etc/nginx') }}/sites-enabled/default"
    state: absent
  when: ansible_os_family == "Debian"

- name: 生成默认站点配置（可选）
  ansible.builtin.template:
    src: default.conf.j2
    dest: "{{ nginx_conf_dir | default('/etc/nginx') }}/conf.d/default.conf"
    owner: root
    group: root
    mode: "0644"
  notify: restart nginx
  when: nginx_create_default_conf | default(true) | bool

- name: 启动并启用 Nginx 服务
  nsible.builtin.systemd:
   name: nginx
   state: started
   enabled: true
   daemon_reload: true

# - name: 确保 systemd 服务文件存在
#   ansible.builtin.stat:
#     path: /usr/lib/systemd/system/nginx.service
#   register: nginx_service_file

# - name: 若服务文件缺失则重新安装 Nginx
#   ansible.builtin.yum:
#     name: nginx
#     state: reinstalled
#   when:
#     - not nginx_service_file.stat.exists
#     - ansible_os_family == "RedHat"

# - name: 重新加载 systemd
#   ansible.builtin.systemd:
#     daemon_reload: true

# - name: 启动并启用 Nginx 服务
#   ansible.builtin.systemd:
#     name: nginx
#     state: started
#     enabled: true
#   register: nginx_started
#   retries: 3
#   delay: 5
#   until: nginx_started is success