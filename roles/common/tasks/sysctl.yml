---
# roles/common/tasks/sysctl.yml
# 内核参数调优：部署 sysctl 配置、立即生效，并禁用透明大页

- name: 部署内核参数配置文件
  ansible.builtin.template:
    src: sysctl.conf.j2
    dest: /etc/sysctl.d/99-middleware.conf
    owner: root
    group: root
    mode: "0644"
  notify: reload sysctl

- name: 使新 sysctl 配置立即生效
  ansible.builtin.command: sysctl -p /etc/sysctl.d/99-middleware.conf
  changed_when: false
  when: ansible_virtualization_type != 'docker'   # 容器内执行 sysctl 可能被拒绝

- name: 部署并启动透明大页禁用服务（使用 shell 确保生效）
  block:
    - name: 写入 disable-thp.service 文件
      ansible.builtin.copy:
        dest: /etc/systemd/system/disable-thp.service
        content: |
          [Unit]
          Description=Disable Transparent Huge Pages (THP)
          After=sysinit.target local-fs.target
          Before=basic.target

          [Service]
          Type=oneshot
          ExecStart=/bin/sh -c "echo never > /sys/kernel/mm/transparent_hugepage/enabled && echo never > /sys/kernel/mm/transparent_hugepage/defrag"

          [Install]
          WantedBy=basic.target
        owner: root
        group: root
        mode: "0644"
      register: thp_file

    - name: 重载 systemd 并启动服务
      ansible.builtin.shell: |
        systemctl daemon-reload
        systemctl enable disable-thp
        systemctl start disable-thp || true
      when: thp_file is changed
      register: thp_start
      changed_when: thp_start.rc == 0
  when:
    - ansible_virtualization_type != 'docker'
    - ansible_service_mgr == 'systemd'